import { Container, Typography } from "@mui/material";

const Services = () => {
  return (
    <Container sx={{ my: 4 }}>
      <Typography variant="h4">Servicios</Typography>
      <Typography>
        Consultas, vacunas, cirugías y peluquería
      </Typography>
    </Container>
  );
};

export default Services;
